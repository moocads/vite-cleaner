import { ExternalLink, Mail, MapPin } from "lucide-react";
import { PageShell, SectionHeading } from "@/components/page-shell";
import { locations } from "@/lib/site-data";

export default async function ContactPage({ searchParams }: { searchParams: Promise<{ topic?: string }> }) {
  const { topic } = await searchParams;
  return <PageShell title={topic === "franchise" ? "Start a franchise conversation." : "Talk to the right Vite team."} description="Choose a location, ask about Commercial Laundry or begin a franchise conversation." eyebrow="CONTACT"><section className="bg-vite-paper px-5 py-20 sm:px-8 lg:px-[72px] lg:py-24"><div className="mx-auto max-w-[1296px]"><SectionHeading title="Choose the reason for your message." description="Confirmed phone numbers, email addresses and form endpoints will be connected before launch." /><div className="mt-12 grid gap-5 md:grid-cols-3"><article className="rounded-[24px] bg-vite-blue-soft p-7"><Mail aria-hidden="true" className="size-6 text-vite-cobalt" /><h2 className="mt-6 font-display text-2xl text-vite-navy">Customer Support</h2><p className="mt-3 text-sm leading-6 text-vite-muted">Questions about garment care, pickup, delivery or an existing order.</p></article><article className="rounded-[24px] bg-vite-blue-soft p-7"><Mail aria-hidden="true" className="size-6 text-vite-cobalt" /><h2 className="mt-6 font-display text-2xl text-vite-navy">Commercial Laundry</h2><p className="mt-3 text-sm leading-6 text-vite-muted">Request a tailored commercial service plan and quote.</p></article><article className="rounded-[24px] bg-vite-navy p-7 text-vite-paper"><Mail aria-hidden="true" className="size-6 text-vite-blue-soft" /><h2 className="mt-6 font-display text-2xl">Franchise</h2><p className="mt-3 text-sm leading-6 text-vite-blue-soft/80">Request the official package and discuss franchise opportunities.</p></article></div><div className="mt-14 grid gap-3 sm:grid-cols-3">{locations.filter((location) => location.kind === "Store").map((location) => <a key={location.slug} href={location.mapUrl} target="_blank" rel="noreferrer" className="flex items-start gap-4 rounded-[18px] border border-vite-line bg-vite-surface p-5"><MapPin aria-hidden="true" className="mt-0.5 size-5 shrink-0 text-vite-cobalt" /><span><span className="font-display text-lg text-vite-navy">{location.name}</span><span className="mt-1 block text-xs leading-5 text-vite-muted">{location.address}</span></span><ExternalLink aria-hidden="true" className="ml-auto size-4 text-vite-cobalt" /></a>)}</div></div></section></PageShell>;
}
